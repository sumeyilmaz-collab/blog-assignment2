---
import MainLayout from "../layouts/MainLayout.astro";
import { posts } from "../data/posts";

const categories = ["All", "Adventure", "Travel", "Fashion", "Technology", "Design"];
---

<MainLayout title="Posts | The Sumeyye Journal" fullWidth={true}>
  <!-- HERO -->
  <section class="w-full bg-gradient-to-r from-slate-950 via-slate-900 to-emerald-950 py-24 text-center text-white">
    <div class="max-w-5xl mx-auto px-6">
      <h1 class="text-5xl font-bold tracking-tight">Posts</h1>
      <p class="mt-4 text-lg text-slate-300 max-w-2xl mx-auto">
        Explore the universe of Web, design and technology through articles.
      </p>
    </div>
  </section>

  <!-- CONTENT -->
  <section class="bg-[#fbf8f3] py-14">
    <div class="max-w-6xl mx-auto px-6">
      
      <!-- CATEGORY FILTER -->
      <div class="flex gap-6 text-sm font-medium text-slate-600 mb-10">
        {categories.map((cat) => (
          <button class={`hover:text-blue-600 ${cat === "All" ? "text-blue-600 font-semibold" : ""}`}>
            {cat}
          </button>
        ))}
      </div>

      <!-- POSTS GRID -->
      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {posts.map((post) => (
          <a
            href={`/posts/${post.slug}`}
            class="group bg-white rounded-xl border border-slate-200 shadow-sm hover:shadow-lg transition overflow-hidden flex flex-col h-full"
          >
            <div class="h-56 w-full overflow-hidden">
              <img
                src={post.image}
                alt={post.title}
                class="h-full w-full object-cover group-hover:scale-105 transition duration-300"
              />
            </div>

            <div class="p-6 flex flex-col flex-1">
              <p class="text-xs font-semibold text-blue-600 uppercase tracking-wide">
                {post.category}
              </p>

              <h3 class="mt-2 text-lg font-bold text-slate-900 group-hover:text-blue-700 transition">
                {post.title}
              </h3>

              <p class="mt-3 text-sm text-slate-600 leading-relaxed">
                {post.desc}
              </p>

              <div class="mt-auto pt-4 text-xs text-slate-500 flex items-center gap-2">
                <span class="font-semibold">{post.author}</span>
                <span>•</span>
                <span>{post.date}</span>
              </div>
            </div>
          </a>
        ))}
      </div>

      <!-- BUTTON -->
      <div class="flex justify-center mt-14">
        <button class="px-8 py-3 rounded-md bg-blue-600 text-white font-semibold shadow hover:bg-blue-700 transition">
          View All Post
        </button>
      </div>

    </div>
  </section>
</MainLayout>
